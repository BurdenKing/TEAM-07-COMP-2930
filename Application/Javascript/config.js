
//Make sure your localhost has this user and password information.
//Otherwise, you will need to create an account on PHPMyAdmin
let config = {
    host: 'localhost',
    user: 'root',
    password: 'simple',
    database: 'parkaway_project'
};

module.exports = config;